export interface IJobs {
  id: number;
  title: string;
  description: string;
  longDescription: string;
}

export type Jobs = {
  id: string;
  title: string;
  description: string;
  longDescription: string;
};
